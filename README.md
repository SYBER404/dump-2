    • simpel dump teman facebook
    • login with cookies facebook

    $ pkg update
    $ pkg install python
    $ pkg install git
    $ pip install rich
    $ pip install requests
    $ git clone https://github.com/khamdihi-dev/dump
    $ cd dump
    $ python dump.py
